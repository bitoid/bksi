{#
/**
 * @file
 * Theme override for an 'input' #type form element.
 *
 * Available variables:
 * - attributes: A list of HTML attributes for the input element.
 * - children: Optional additional rendered elements.
 *
 * @see template_preprocess_input()
 */
#}

{%
    set classes = [
        'hidden',
    ]
%}

<label class=" flex  md:mb-[18px] mb-[30px] gap-1" for="file"><span class="font-['halbfett'] "> Datenupload </span> (PDF-Dateien, max. 5MB) Unterlagen (z.B. Lebenslauf)</label>
<label class="w-[220px] h-10 bg-white rounded-[38px] font-bold flex justify-center items-center" for="file">Datei auswählen</label>
<input{{ attributes.addClass(classes).setAttribute('id', 'file') }} />{{ children }}