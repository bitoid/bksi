{#
/**
 * @file
 * Theme override for an 'input' #type form element.
 *
 * Available variables:
 * - attributes: A list of HTML attributes for the input element.
 * - children: Optional additional rendered elements.
 *
 * @see template_preprocess_input()
 */
#}

{%
    set classes = [
        'hidden',
    ]
%}

<label class=" flex  md:mb-[10px] mb-[10px] gap-1" for="file"><span class="font-['halbfett'] "> {{ 'Datenupload' | t}}</span> {{ '(PDF-Dateien, max. 5MB) Unterlagen (z.B. Lebenslauf)' | t }}</label>
<label class="w-[220px] h-10 text-[15px] leading-[19px] tracking-[0.75px]  bg-white rounded-[38px] font-bold flex justify-center items-center" for="file">{{ 'Datei auswählen' | t }}</label>
<input{{ attributes.addClass(classes).setAttribute('id', 'file') }} />{{ children }}