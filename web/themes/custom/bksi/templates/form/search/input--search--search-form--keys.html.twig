{#
/**
 * @file
 * Theme override for an 'input' #type form element.
 *
 * Available variables:
 * - attributes: A list of HTML attributes for the input element.
 * - children: Optional additional rendered elements.
 *
 * @see template_preprocess_input()
 */
#}

{%
    set classes = [ 
        'max-w-[250px]',
        'rounded-xl', 
        'md:max-w-[300px]',
        'font-["halbfett"]',
        'outline-none',
        'flex-grow'
    ]
%}

<div class="">
    <div class="text-[6vw] md:text-[35px] gap-[15px] flex md:flex-row flex-col justify-center px-4 pb-[50px] md:max-w-[842px] md:mx-auto ">
        <h2 class="md:ml-[-52px]">{{ 'Suchergebnisse für:' | t}}</h2>
        <div class="text-[6vw]
        md:text-[35px]">
            <span><</span>
            <input{{ attributes.addClass(classes) }} />{{ children }}
            <span>></span>
        </div>
    </div>
</div>

