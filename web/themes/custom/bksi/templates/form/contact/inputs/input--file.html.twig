{#
/**
 * @file
 * Theme override for an 'input' #type form element.
 *
 * Available variables:
 * - attributes: A list of HTML attributes for the input element.
 * - children: Optional additional rendered elements.
 *
 * @see template_preprocess_input()
 */
#}

{%
    set classes = [
        'hidden',
    ]
%}

<label class="font-bold" for="file">Datenupload (PDF-Dateien, max. 5MB) Unterlagen (z.B. Lebenslauf)</label>
<label class="w-[220px] h-10 bg-white rounded-[38px] flex justify-center items-center" for="file">Datei auswählen</label>
<input{{ attributes.addClass(classes).setAttribute('id', 'file') }} />{{ children }}